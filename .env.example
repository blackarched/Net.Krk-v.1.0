# Configuration for the net.krak application
# The host the API server will listen on. 0.0.0.0 is recommended for Docker.
API_HOST=0.0.0.0

# The port for both the API and the web dashboard.
API_PORT=5000