# Network Scanner and ARP Spoofing Tool Dependencies
# ==================================================

# Core networking and packet manipulation
scapy>=2.5.0

# HTTP requests for MAC vendor lookup
requests>=2.28.0

# Additional dependencies that may be required
# (These are often system-level dependencies)
# sudo apt-get install python3-dev libpcap-dev
# sudo apt-get install tcpdump
# sudo apt-get install net-tools

# Optional: For better performance and additional features
# psutil>=5.9.0  # For system and process monitoring
# colorama>=0.4.6  # For colored terminal output